# Java exercise sets

Create one subdirectory per exercise set, using the same set name referenced by
the profile. Each set directory must contain `manifest.json` and its listed
`.java` source files.

Example:

```text
java/printing/manifest.json
java/printing/PrintLine.java
```
